# Screenshots

Placeholder. Drop PNG screenshots here when ready:

- `01-first-run-config.png` — the Configure Open Brain screen (popup/config.html) on first install
- `02-popup-capture.png` — the main popup showing the Capture Current Response button on a Claude tab
- `03-activity-log.png` — the overview tab with a few successful + skipped captures
- `04-sync-tab.png` — the Claude/ChatGPT sync controls

Each screenshot should be under 500KB. Reference them from `../../README.md`.
